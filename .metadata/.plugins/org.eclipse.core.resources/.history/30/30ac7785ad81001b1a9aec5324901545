package demo;

public class Recursion {
/*
 * 什么是递归
 * 递归就是在运行中调用自己
 * 
 * */
	
	static int recursion(int num) {
		if(num>1) {
			num += recursion(num-1);
			return num;
		}else {
			return num;
		}
	}
}
